import { FormError } from './form-error';

describe('FormError', () => {
  it('should create an instance', () => {
    const directive = new FormError();
    expect(directive).toBeTruthy();
  });
});
